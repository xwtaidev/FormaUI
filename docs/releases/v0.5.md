# FormaUI v0.5 Release Notes

Date: 2026-04-26  
Status: GA  
Version Line: v0.5.1 -> v0.5.6

## TL;DR

FormaUI v0.5 完成了官网与文档站拆分：`apps/web` 聚焦品牌与增长叙事，`apps/docs` 聚焦开发者文档与迁移指南。  
在 v0.5.5 中已完成跨站回归测试扩展与全仓质量门禁验证，v0.5.6 已完成 GA 前人工核对并正式收口。

## Route Inventory (PRD Alignment)

### Web Responsibilities (`apps/web`)

| PRD Route | Delivered Route | Status |
| --- | --- | --- |
| Home | `/` | PASS |
| Marketing | `/marketing` | PASS |
| Product | `/product` | PASS |
| Scenarios | `/scenarios` | PASS |
| Showcase | `/showcase` | PASS |
| Blog | `/blog` | PASS |
| Changelog (promotion) | `/changelog` | PASS |

### Docs Responsibilities (`apps/docs`)

| PRD Route Class | Delivered Route | Status |
| --- | --- | --- |
| Installation | `/installation` | PASS |
| Components | `/components/*` | PASS |
| Blocks | `/blocks/*` | PASS |
| Templates | `/templates/*` | PASS |
| CLI | `/cli` | PASS |
| Registry | `/registry` | PASS |
| Migration Guide | `/migration-v0.4-to-v0.5` | PASS |

## Migration Redirect Inventory (Docs -> Web)

Legacy marketing routes in docs are permanently redirected to web:

| Legacy Route (`apps/docs`) | Destination (`apps/web`) |
| --- | --- |
| `/landing` | `https://formaui.com/` |
| `/landing/architecture` | `https://formaui.com/product` |
| `/landing/reuse-blocks` | `https://formaui.com/showcase` |
| `/landing/:path*` | `https://formaui.com/` |

Cross-site navigation baseline:

- Web global shell provides `Docs` entry (`Web -> Docs`).
- Docs sidebar provides `Open Web Site` entry (`Docs -> Web`).

## Validation Gates

Release gating command (v0.5.5):

```bash
pnpm lint && pnpm typecheck && pnpm test && pnpm build
```

Result snapshot:

- `pnpm lint`: PASS
- `pnpm typecheck`: PASS
- `pnpm test`: PASS
- `pnpm build`: PASS

Supporting quality evidence from v0.5.5 regression expansion:

- `apps/web/tests/route-coverage.test.tsx`: cross-site nav + changelog backlink coverage.
- `apps/web/tests/seo-routing.test.ts`: metadata/sitemap/robots + release/migration backlinks.
- `apps/docs/tests/split-routing.test.ts`: redirect map + docs-to-web entry coverage.
- `apps/docs/tests/landing-seo.test.ts`: docs-critical sitemap/robots route coverage.

## Subversion Delivery Map

| Subversion | Focus | Result |
| --- | --- | --- |
| v0.5.1 | Workspace Split Foundation | `apps/web` scaffold and workspace integration complete |
| v0.5.2 | Web IA Delivery | Web required route set delivered |
| v0.5.3 | Docs Scope Convergence | Docs scope converged to developer documentation |
| v0.5.4 | Routing & SEO Migration | Redirect map + web SEO outputs + changelog linkage complete |
| v0.5.5 | Quality Gates | Split-site regression suite and full repo gates passed |
| v0.5.6 | GA Cutover Checklist | Manual verification completed and release notes finalized |

## GA Manual Verification Checklist (v0.5.6)

Verification date: 2026-04-26

### 1. Web 7 Routes and Docs 7 Route Classes Reachability

Validation command set:

```bash
pnpm --filter web build
pnpm --filter docs build
```

Result:

- Web route table includes all required routes: `/`, `/marketing`, `/product`, `/scenarios`, `/showcase`, `/blog`, `/changelog`.
- Docs build output includes required route classes with concrete entries:
  - Installation: `/installation`
  - Components: `/components/button` (and full component set)
  - Blocks: `/blocks/dashboard-shell` (and full block set)
  - Templates: `/templates/ai-console-lite` (and template set)
  - CLI: `/cli`
  - Registry: `/registry`
  - Migration: `/migration-v0.4-to-v0.5`

Checklist status: PASS

### 2. Cross-site Links Visibility on Desktop and Mobile

Validation command set:

```bash
pnpm --filter web test -- --run tests/route-coverage.test.tsx
pnpm --filter docs test -- --run tests/split-routing.test.ts
rg -n "Docs|Open Web Site|hidden|lg:hidden|md:hidden|sm:hidden" apps/web/app/layout.tsx apps/docs/app/layout.tsx
```

Result:

- `Web -> Docs`: web global shell renders `Docs` link.
- `Docs -> Web`: docs sidebar renders `Open Web Site` link.
- No responsive utility class (`hidden`, `lg:hidden`, `md:hidden`, `sm:hidden`) is applied to these cross-site entries.

Checklist status: PASS

### 3. Legacy Route Redirect Sampling (10 Paths)

Validation command:

```bash
node -e 'import("./apps/docs/next.config.mjs").then(async (m)=>{const redirects=await m.default.redirects();const samples=["/landing","/landing/architecture","/landing/reuse-blocks","/landing/legacy","/landing/legacy/deep/path","/landing/pricing","/landing/features","/landing/case-studies/ai","/landing/compare/formaui-v0.4","/landing/docs-entry"];const resolve=(path)=>{const exact=redirects.find(r=>r.source===path);if(exact)return exact.destination;const wildcard=redirects.find(r=>r.source==="/landing/:path*");if(wildcard&&path.startsWith("/landing/"))return wildcard.destination;return null;};for(const p of samples){console.log(`${p} -> ${resolve(p) ?? "NO_MATCH"}`);}});'
```

Sample results:

| Sample Path | Redirect Target | Status |
| --- | --- | --- |
| `/landing` | `https://formaui.com/` | PASS |
| `/landing/architecture` | `https://formaui.com/product` | PASS |
| `/landing/reuse-blocks` | `https://formaui.com/showcase` | PASS |
| `/landing/legacy` | `https://formaui.com/` | PASS |
| `/landing/legacy/deep/path` | `https://formaui.com/` | PASS |
| `/landing/pricing` | `https://formaui.com/` | PASS |
| `/landing/features` | `https://formaui.com/` | PASS |
| `/landing/case-studies/ai` | `https://formaui.com/` | PASS |
| `/landing/compare/formaui-v0.4` | `https://formaui.com/` | PASS |
| `/landing/docs-entry` | `https://formaui.com/` | PASS |

Checklist status: PASS

### 4. GA Conclusion

- v0.5 PRD route-split acceptance items are covered by automated regression and manual checklist records.
- No blocker found in cross-site navigation, legacy route migration, or route reachability.
- v0.5 is ready for GA cutover.

## References

- PRD: `docs/prds/FormaUI v0.5 PRD.md`
- Plan: `docs/superpowers/plans/2026-04-26-formaui-v0.5-implementation-plan.md`
- Prior release: `docs/releases/v0.4.md`
